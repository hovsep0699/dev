import { IAggregatedFlow } from '../types';

const UCDInvoice: IAggregatedFlow = {
  name: 'universal_correction_document',
  label: 'Универсальный корректировочный документ',
  shortLabel: 'УКД (СЧФ)'
};

export default UCDInvoice;
