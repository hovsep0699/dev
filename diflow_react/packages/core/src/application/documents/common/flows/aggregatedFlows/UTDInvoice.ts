import { IAggregatedFlow } from '../types';

const UTDInvoice: IAggregatedFlow = {
  name: 'universal_invoice',
  label: 'Универсальный передаточный документ',
  shortLabel: 'УПД (СЧФ)'
};

export default UTDInvoice;
