import { IAggregatedState } from '../../types';

const SigningPdpolNotification: IAggregatedState = {
  name: 'signing_pdpol_notification',
  label: 'Подписание квитанций',
  type: 'OUT'
};

export default SigningPdpolNotification;
