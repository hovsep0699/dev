import { IAggregatedFlow } from '../types';

const UTDWaybill: IAggregatedFlow = {
  name: 'utd_waybill',
  label: 'Универсальный передаточный документ',
  shortLabel: 'УПД (ДОП)'
};

export default UTDWaybill;
