import { IFlow } from '../types';

const UnformalizedUnilateralUnsigned: IFlow = {
  name: 'unformalized_unilateral_unsigned',
  label: 'Выставленные счета по ЭДО',
  type: 'local'
};

export default UnformalizedUnilateralUnsigned;
