import React from 'react';
import { NO_CERTS } from '../../../../common/Lbl';

const NoCert = () => (
  <div className="text-center" style={{ height: '64px', lineHeight: '64px' }}>
    <h1>{NO_CERTS}</h1>
  </div>
);

export default NoCert;
